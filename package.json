{
  "name": "fedstack",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production-quality React + TypeScript multi-app setup with TanStack Router and Module Federation",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm dev:remote\" \"pnpm dev:analytics\" \"pnpm dev:host\"",
    "dev:host": "cd apps/host && pnpm dev",
    "dev:remote": "cd apps/remote && pnpm dev",
    "dev:analytics": "cd apps/analytics && pnpm dev",
    "dev:versions": "concurrently \"pnpm serve:versions\" \"VITE_USE_VERSIONED=true pnpm dev:host\"",
    "build": "pnpm build:remote && pnpm build:analytics && pnpm build:host",
    "build:host": "cd apps/host && pnpm build",
    "build:remote": "cd apps/remote && pnpm build",
    "build:analytics": "cd apps/analytics && pnpm build",
    "build:versions": "./scripts/build-versioned.sh analytics 1.0.0 && ./scripts/build-versioned.sh analytics 1.1.0",
    "build:version": "./scripts/build-versioned.sh",
    "promote:auto": "./scripts/promote-auto.sh",
    "prepare:next": "./scripts/prepare-next.sh",
    "serve:versions": "node scripts/serve-versions.js",
    "test": "pnpm -r test",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "pnpm lint --fix",
    "format": "prettier --write .",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf node_modules dist-versions"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react": "^7.34.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "prettier": "^3.2.5",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
